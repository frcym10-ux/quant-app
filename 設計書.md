# クオンツ投資分析アプリ 設計書

作成日: 2026-06-11  
バージョン: 1.0

---

## 1. プロジェクト目標

個人投資家（自分）が日本株・米国株を定量的に分析するためのWebアプリを完全無料で構築する。

**メイン機能**
- C. 銘柄スクリーニング：テクニカル条件でフィルタリング
- D. テクニカル分析チャート：複数指標を1画面に統合表示

---

## 2. 技術スタック

| 層 | 技術 | 理由 |
|----|------|------|
| UI | Streamlit | Pythonだけ、無料、手軽 |
| 日本株データ | J-Quants API | JPX公式、無料プランあり |
| 米国株データ | yfinance | 完全無料 |
| テクニカル指標 | pandas-ta | インストール簡単、指標豊富 |
| グラフ | Plotly | インタラクティブ、無料 |
| ローカルDB | SQLite | サーバー不要 |

---

## 3. 画面一覧

### 3-1. トップページ（app.py）
- アプリの概要説明
- 各機能へのナビゲーション

### 3-2. 銘柄スクリーニング（pages/1_screener.py）

**入力エリア（サイドバー）**
- 市場選択（東証プライム / 米国ETF）
- ADX閾値スライダー（レンジ / トレンド判定）
- RSI条件（売られすぎ / 買われすぎ）
- BB条件（上限タッチ / 下限タッチ）
- Keltner条件

**出力エリア**
- テーブル：銘柄コード・銘柄名・現在値・RSI・ADX・レジーム判定・シグナル種別
- 行をクリック → チャート画面へ遷移

### 3-3. テクニカル分析チャート（pages/2_chart.py）

**入力エリア**
- 銘柄コード入力（例: 7011, VOO）
- 期間選択（1ヶ月 / 3ヶ月 / 6ヶ月 / 1年）
- 表示する指標のチェックボックス

**チャートエリア（Plotly、4段構成）**

```
[ローソク足 + BB + Keltner + VWAP + EMA]  ← メイン（高さ大）
[MACDヒストグラム + MACDライン + Signal]  ← サブ1
[RSI + 70/30ライン]                       ← サブ2
[ADX + 20/25ライン]                       ← サブ3
```

### 3-4. 戦略シグナル（pages/3_strategy.py）
- 銘柄を選択して戦略α/βのシグナル履歴を表示
- シグナル発生日・種別・エントリー価格・SL/TP価格

### 3-5. ポートフォリオ概況（pages/4_portfolio.py）
- 保有銘柄一覧の現在値・損益
- 各銘柄の現在のレジーム（レンジ/トレンド）

---

## 4. モジュール詳細

### data_fetcher.py

```python
def get_jp_stock(code: str, days: int) -> pd.DataFrame:
    """J-Quants APIで日本株の日足データを取得"""

def get_us_stock(ticker: str, days: int) -> pd.DataFrame:
    """yfinanceで米国株の日足データを取得"""

def get_cached_or_fetch(code: str, days: int) -> pd.DataFrame:
    """キャッシュがあれば返し、なければ取得してキャッシュする"""
```

### indicators.py

```python
def calc_all(df: pd.DataFrame) -> pd.DataFrame:
    """全テクニカル指標を一括計算して列追加して返す"""
    # 計算する指標：
    # - MACD (12, 26, 9)
    # - RSI (14)
    # - Bollinger Bands (20, 2.0)
    # - Keltner Channels (20, 10, 2.0)
    # - ATR (14)
    # - ADX (14)
    # - EMA (12, 26)
    # - VWAP（日次データでは近似計算）
```

### screener.py

```python
def screen(universe: list[str], conditions: dict) -> pd.DataFrame:
    """条件に合致する銘柄を返す"""

def get_regime(adx: float) -> str:
    """ADX値からレジームを判定して返す（'レンジ' or 'トレンド'）"""
```

### strategy_alpha.py

```python
def signal(df: pd.DataFrame) -> pd.Series:
    """戦略αのシグナルを返す（1=ロング, -1=ショート, 0=なし）"""
```

### strategy_beta.py

```python
def signal(df: pd.DataFrame) -> pd.Series:
    """戦略βのシグナルを返す（1=ロング, 0=なし）"""
```

### risk_manager.py

```python
def calc_sl_tp(entry: float, atr: float, direction: int) -> tuple[float, float]:
    """ATRベースのSL/TPを計算して返す"""

def calc_position_size(account: float, risk_pct: float, entry: float, sl: float) -> float:
    """ATRポジションサイジングで購入株数を計算"""
```

---

## 5. データフロー

```
[J-Quants API] ──→ data_fetcher.py ──→ SQLiteキャッシュ
[yfinance]     ──→                   ──→ indicators.py ──→ df（指標付き）
                                                          ↓
                                         screener.py ──→ スクリーニング結果
                                         strategy_alpha.py ──→ シグナル
                                         strategy_beta.py  ──→ シグナル
                                                          ↓
                                         Plotlyチャート ──→ Streamlit表示
```

---

## 6. 開発スケジュール（目安）

| Phase | 内容 | 優先度 |
|-------|------|--------|
| 1 | data_fetcher.py 実装・J-Quants接続確認 | 最高 |
| 2 | indicators.py 実装・指標計算確認 | 高 |
| 3 | チャート画面（4段Plotly）実装 | 高 |
| 4 | スクリーニング画面実装 | 中 |
| 5 | strategy_alpha / beta 実装 | 中 |
| 6 | risk_manager.py 実装 | 中 |
| 7 | ポートフォリオ画面 | 低 |

---

## 7. J-Quants APIの登録方法

1. https://jpx-jquants.com/ にアクセス
2. 「無料で始める」から会員登録（メールアドレスのみ）
3. ログイン後、「APIキー」→「リフレッシュトークン」をコピー
4. `.env` ファイルの `JQUANTS_REFRESH_TOKEN=` にペースト

**無料プランの制限**
- 日足OHLCV: 直近12週分
- 財務データ: 利用可能
- 銘柄情報: 利用可能

---

## 8. 参考レポート

「クオンツ・アプローチに基づくテクニカル指標の深層分析とアルゴリズム構築ロジック」
（Gemini生成レポート、2026年6月）

主要参考箇所：
- 戦略αロジック（VWAP統合型BBモメンタム反転）
- 戦略βロジック（ケルトナーチャネル+ADX自己適応型平均回帰）
- ATRベースリスク管理の定式化
- 保有銘柄へのレジーム適用ガイドライン
